@model ExceptionLoggerGetDto
@{
	ViewData["Title"] = "Exception Log Detail";
	ViewData["Subtitle"] = "Manage your exception logs";
	Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid px-4 py-3">
	<div class="d-flex justify-content-between align-items-center mb-4">
		<div>
			<h2 class="fw-bold mb-1">@ViewData["Title"]</h2>
			<p class="text-muted mb-0">@ViewData["Subtitle"]</p>
		</div>
		<div>
			<a asp-controller="ExceptionLogger" asp-action="WholeLogs" class="btn btn-info">
				Whole Exception Logs
			</a>
			<a asp-controller="ExceptionLogger" asp-action="Index" class="btn btn-outline-secondary me-2">
				<i class="bi bi-arrow-left me-1"></i> Back to Exception Log List
			</a>
			<a asp-controller="Category" asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-success">
				<i class="bi bi-pencil me-1"></i> Edit
			</a>
		</div>
	</div>

	<div class="row g-4">
		<div class="col-md-8">
			<div class="card shadow-sm">
				<div class="card-header bg-white py-3">
					<h6 class="fw-bold mb-0">General Information</h6>
				</div>
				<div class="card-body">
					<dl class="row mb-0">
						<dt class="col-sm-3 text-muted fw-normal">Controller Name</dt>
						<dd class="col-sm-9 fw-semibold">@Model.ControllerName</dd>

						<dt class="col-sm-3 text-muted fw-normal">Exception Message</dt>
						<dd class="col-sm-9">@Model.ExceptionMessage.WrapText(120)</dd>

						<dt class="col-sm-3 text-muted fw-normal">Exception Stack Trace</dt>
						<dd class="col-sm-9">@Model.ExceptionStackTrace.WrapText(120)</dd>

						<dt class="col-sm-3 text-muted fw-normal">Status</dt>
						<dd class="col-sm-9">
							@if (Model.IsActive)
							{
								<span class="badge bg-success-subtle text-success px-3 py-2">Active</span>
							}
							else
							{
								<span class="badge bg-danger-subtle text-danger px-3 py-2">Inactive</span>
							}
							@if (Model.IsDeleted)
							{
								<span class="badge bg-success-subtle text-danger px-3 py-2">Deleted</span>
							}
							else
							{
								<span class="badge bg-danger-subtle text-success px-3 py-2">Not Deleted</span>
							}
						</dd>

						<dt class="col-sm-3 text-muted fw-normal">Created At</dt>
						<dd class="col-sm-9">@Model.CreatedDate.ToString("dd MMM yyyy, HH:mm")</dd>

						<dt class="col-sm-3 text-muted fw-normal">Updated At</dt>
						<dd class="col-sm-9">
							@(Model.UpdatedDate.HasValue? Model.UpdatedDate.Value.ToString("dd MMM yyyy, HH:mm") : "-")
						</dd>

						<dt class="col-sm-3 text-muted fw-normal">Suspended At</dt>
						<dd class="col-sm-9">
							@(Model.SuspendedDate.HasValue? Model.SuspendedDate.Value.ToString("dd MMM yyyy, HH:mm") : "-")
						</dd>

						<dt class="col-sm-3 text-muted fw-normal">Deleted At</dt>
						<dd class="col-sm-9">
							@(Model.DeletedDate.HasValue? Model.DeletedDate.Value.ToString("dd MMM yyyy, HH:mm") : "-")
						</dd>
					</dl>
				</div>
			</div>
		</div>

		<div class="col-md-4">
			<div class="card shadow-sm">
				<div class="card-header bg-white py-3">
					<h6 class="fw-bold mb-0">Actions</h6>
				</div>
				<div class="card-body d-grid gap-2">
					<form asp-controller="ExceptionLogger" asp-action="Delete" method="post" onsubmit="return confirm('Are you sure you want to delete this log?')">
						@Html.AntiForgeryToken()
						<input type="hidden" name="id" value="@Model.Id" />
						<button type="submit" class="btn btn-outline-danger w-100">
							<i class="bi bi-trash me-2"></i> Delete Exception Log
						</button>
					</form>
				</div>
			</div>
		</div>
	</div>
</div>